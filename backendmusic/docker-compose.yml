services:
  backend:
    build:
      context: .
      dockerfile: DockerFile
    command: 'python manage.py runserver 0.0.0.0:8000'
    ports:
      - 8000:8000
    volumes:
      - .:/app
    # depends_on:
    #   - db
  
  # db:
  #   image: mysql:8.0.32
  #   restart: always
  #   environment:
  #     MYSQL_DATABASE: reacting
  #     MYSQL_USER: root
  #     MYSQL_PASSWORD: Misha20088mamont
  #     MYSQL_ROOT_PASSWORD: Misha20088mamont
  #   volumes:
  #     - .dbdata:/var/lib/mysql
  #   ports:
  #     - :33066:3306